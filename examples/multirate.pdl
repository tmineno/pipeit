# multirate.pdl — Multi-rate spectrum analyzer
# ==============================================
# Demonstrates: SDF balance with different token rates,
#               tap/fork branching, type checking (float vs cfloat)

const lp_coeff = [0.25, 0.5, 0.25]

clock 1kHz analyzer {
    constant(0.0) | :sig | fir(lp_coeff) | stdout()
    :sig | fft(64) | mag() | stdout()
}
