# feedback.pdl — IIR filter with feedback loop
# =============================================
# Demonstrates: feedback delay verification, multi-input actor,
#               tap-input syntax, SDF balance with add(IN(float,2))

clock 1kHz iir {
    adc(0) | add(:fb) | :out | stdout()
    :out | delay(1, 0.0) | :fb
}
