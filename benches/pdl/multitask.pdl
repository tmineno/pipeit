# Multi-task benchmark - shared buffer communication
# Tests cross-task rate matching via decimation.

clock 10kHz producer {
    constant(0.0) | mul(2.0) -> data
}

clock 5kHz consumer {
    @data | decimate(2) | mul(0.5) | delay(1, 0.0) | stdout()
}
